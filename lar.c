#include <stdio.h>

int main()
{
     int n1=5,n2=4,n3=6;
    if(n1>=n2 && n1>=n3)
    printf("n1 the largest number.",n1);
    if(n2>=n1 && n2>=n3)
    printf("n2 is the largest number.", n2);
    if(n3>=n1 && n3>=n2)
    printf("n3 is the largest number.", n3);
    
}
